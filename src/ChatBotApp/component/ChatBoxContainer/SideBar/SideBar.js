import React, { Component } from "react";
import { Layout } from "antd";

export class SideBar extends Component {
  render() {
    const { Sider } = Layout;
    return <Sider>side</Sider>;
  }
}

export default SideBar;
